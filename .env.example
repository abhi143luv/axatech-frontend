# Backend base URL without /api (e.g. https://axatech-backend.vercel.app). /api is added automatically.
# Leave unset for local dev (Vite proxies /api to localhost:5000).
# For Vercel: set in Project → Settings → Environment Variables.
VITE_API_BASE_URL=https://axatech-backend.vercel.app
